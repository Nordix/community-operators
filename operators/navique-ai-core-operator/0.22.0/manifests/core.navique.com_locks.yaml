apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: locks.core.navique.com
spec:
  group: core.navique.com
  names:
    kind: Lock
    listKind: LockList
    plural: locks
    shortNames:
    - lk
    singular: lock
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.kind
      name: Target-Kind
      type: string
    - jsonPath: .spec.targetRef.name
      name: Target-Name
      type: string
    - jsonPath: .status.active
      name: Active
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Lock protects a referenced core.navique.com resource from accidental
          deletion.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LockSpec protects a single resource from deletion — Azure "CanNotDelete"
              lock semantics. While a `Lock` exists, the operator stamps its target with a
              lock annotation and a cluster ValidatingAdmissionPolicy rejects every DELETE
              request for that object: you must remove the `Lock` first. Multiple Locks may
              protect the same target; the target stays protected until the last one is
              gone.

              It is a guardrail against accidental deletion, NOT a security boundary —
              anyone with RBAC to delete the `Lock` (or edit the target's annotation) can
              lift it.
            properties:
              reason:
                description: |-
                  Reason is an optional human-readable note explaining why the lock exists.
                  It is surfaced in the rejected-delete message and the Lock's status.
                type: string
              targetRef:
                description: |-
                  TargetRef identifies the resource to protect. It must be a
                  core.navique.com resource in the SAME namespace as the Lock.
                properties:
                  apiVersion:
                    default: core.navique.com/v1alpha1
                    description: |-
                      APIVersion of the target, e.g. "core.navique.com/v1alpha1". Defaults to the
                      operator's group/version; only that group is enforceable.
                    type: string
                  kind:
                    description: Kind of the target, e.g. "Stack", "PostgresCluster",
                      "Gateway".
                    type: string
                  name:
                    description: Name of the target resource in the Lock's namespace.
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - targetRef
            type: object
          status:
            description: LockStatus reports whether the protection is in force.
            properties:
              active:
                description: |-
                  Active is true once the target carries the lock annotation, i.e. deletion
                  of the target is being rejected.
                type: boolean
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
